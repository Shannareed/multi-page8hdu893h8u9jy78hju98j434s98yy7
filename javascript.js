function output(message) { message="Hello Everyone! If you are seeing this message I have properly linked my utility function from a JavaScript file using the script tag and the src attribute."
                          
                            
                       
  window.outputTag.innerHTML = window.outputTag.innerHTML + message;
}